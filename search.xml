<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Linux命令</title>
    <url>/2019/09/25/Linux%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h1 id="文件管理"><a href="#文件管理" class="headerlink" title="文件管理"></a>文件管理</h1><h2 id="cp"><a href="#cp" class="headerlink" title="cp"></a>cp</h2><p> Copy file 主要用于复制文件或目录</p>
<h3 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cp [选项] [参数]</span><br></pre></td></tr></table></figure>

<h3 id="选项"><a href="#选项" class="headerlink" title="选项"></a>选项</h3><ul>
<li><code>-a</code>：此选项通常在复制目录时使用，它保留链接、文件属性，并复制目录下的所有内容。其作用等于-dpR参数组合。</li>
<li><code>-d</code>：复制时保留链接。这里所说的链接相当于Windows系统中的快捷方式。</li>
<li><code>-f</code>：覆盖已经存在的目标文件而不给出提示。</li>
<li><code>-i</code>：与-f选项相反，在覆盖目标文件之前给出提示，要求用户确认是否覆盖，回答”y”时目标文件将被覆盖。</li>
<li><code>-p</code>：除复制文件的内容外，还把修改时间和访问权限也复制到新文件中。</li>
<li><code>-r</code>：若给出的源文件是一个目录文件，此时将复制该目录下所有的子目录和文件。</li>
<li><code>-l</code>：不复制文件，只是生成链接文件。</li>
</ul>
<h3 id="实例"><a href="#实例" class="headerlink" title="实例"></a>实例</h3><ul>
<li><p>使用指令”cp”将当前目录”test/“下的所有文件复制到新目录”newtest”下：</p>
<ul>
<li>注意：用户使用该指令复制目录时，必须使用参数”-r”或者”-R”</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cp –r test&#x2F; newtest  </span><br></pre></td></tr></table></figure>
</li>
<li><p>将一个路径下的多个文件复制到另一路径</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cp C2v&#x2F;&#123;7_C2v.run, adf17.sub&#125; ........</span><br></pre></td></tr></table></figure>
</li>
<li><p>交互式地将目录/usr/men中的以m打头的所有.c文件复制到目录/usr/zh中</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">cp -i &#x2F;usr&#x2F;men m*.c &#x2F;usr&#x2F;zh</span><br></pre></td></tr></table></figure>







</li>
</ul>
<p>将文件file1复制为file2：【会将一个已经存在的文件file1复制并重命名为file2】</p>
<h1 id="文档编辑"><a href="#文档编辑" class="headerlink" title="文档编辑"></a>文档编辑</h1><h1 id="文件传输"><a href="#文件传输" class="headerlink" title="文件传输"></a>文件传输</h1><h1 id="磁盘管理"><a href="#磁盘管理" class="headerlink" title="磁盘管理"></a>磁盘管理</h1><h1 id="磁盘维护"><a href="#磁盘维护" class="headerlink" title="磁盘维护"></a>磁盘维护</h1><h1 id="网络通讯"><a href="#网络通讯" class="headerlink" title="网络通讯"></a>网络通讯</h1><h1 id="系统管理"><a href="#系统管理" class="headerlink" title="系统管理"></a>系统管理</h1><h1 id="系统设置"><a href="#系统设置" class="headerlink" title="系统设置"></a>系统设置</h1><h1 id="备份压缩"><a href="#备份压缩" class="headerlink" title="备份压缩"></a>备份压缩</h1><h1 id="设备管理"><a href="#设备管理" class="headerlink" title="设备管理"></a>设备管理</h1><h1 id="其他命令"><a href="#其他命令" class="headerlink" title="其他命令"></a>其他命令</h1>]]></content>
      <categories>
        <category>系统</category>
      </categories>
      <tags>
        <tag>Linux</tag>
      </tags>
  </entry>
  <entry>
    <title>ADF的SCF相关</title>
    <url>/2019/09/23/SCF%E7%9B%B8%E5%85%B3/</url>
    <content><![CDATA[<h1 id="SCF选项"><a href="#SCF选项" class="headerlink" title="SCF选项"></a>SCF选项</h1><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SCF</span><br><span class="line"> Iterations Niter</span><br><span class="line"> Converge SCFcnv &#123; sconv2 &#125;</span><br><span class="line"> </span><br><span class="line"> AccelerationMethod &#123; ADIIS | LISTi | LISTb | fDIIS | LISTf | MESA | SDIIS &#125;</span><br><span class="line"> MESA [NoFDIIS] [NoLISTb] [NoLISTf] [NoLISTi] [NoSDIIS] [NoADIIS]</span><br><span class="line"> ADIIS</span><br><span class="line">   &#123;THRESH1 a1&#125;</span><br><span class="line">   &#123;THRESH2 a2&#125;</span><br><span class="line"> End</span><br><span class="line"> NoADIIS</span><br><span class="line"> DIIS</span><br><span class="line">   &#123;N n&#125;</span><br><span class="line">   &#123;OK ok&#125;</span><br><span class="line">   &#123;cyc cyc&#125;</span><br><span class="line"> End</span><br><span class="line"> Mixing mix</span><br><span class="line"> Mixing1 mix1</span><br><span class="line"></span><br><span class="line"> OldSCF</span><br><span class="line">End</span><br></pre></td></tr></table></figure>

<h2 id="Iterations"><a href="#Iterations" class="headerlink" title="Iterations"></a>Iterations</h2><p><code>Iterations</code>SCF迭代的最大次数，<code>Niter</code>默认是<code>300</code></p>
<h2 id="Converge"><a href="#Converge" class="headerlink" title="Converge"></a>Converge</h2><p><code>Converge</code> ：</p>
<ul>
<li><code>SCFcnv</code>表示停止迭代的收敛限，默认是<code>1e-6</code>。</li>
<li><code>sconv2</code>表示SCF过程难以收敛时起作用的第二个标准，默认是<code>1e-3</code>，如果达到了，仅发出警告，但是继续计算。仍无法达到的时候SCF不收敛，只能用<code>ALLOW</code>来继续</li>
</ul>
<h2 id="AccelerationMethod"><a href="#AccelerationMethod" class="headerlink" title="AccelerationMethod"></a>AccelerationMethod</h2><p><code>AccelerationMethod</code>调用的SCF加速方法，默认情况下为<code>ADIIS</code>，还可以用<code>LISTi</code> | <code>LISTb</code> | <code>fDIIS</code> | <code>LISTf</code> | <code>MESA</code> | <code>SDIIS</code></p>
<h3 id="MESA"><a href="#MESA" class="headerlink" title="MESA"></a>MESA</h3><p><code>AccelerationMethod MESA</code>是ADIIS, fDIIS, LISTb, LISTf, LISTi和 SDIIS方法的组合</p>
<p><code>MESA NoSDIIS</code>代表从其中删掉某一种方法，也可以直接使用该关键词调用MESA，优先级大于<code>AccelerationMethod</code>指定的方法。</p>
<h3 id="ADIIS"><a href="#ADIIS" class="headerlink" title="ADIIS"></a>ADIIS</h3><p>使用ADIIS + SDIIS方法，根据 [F,P] 交换子矩阵中的最大元素<code>ErrMax</code>来确定具体方法：</p>
<ul>
<li>当ErrMax≥<code>a1</code>，只使用A-DIIS系数</li>
<li>当ErrMax≤ <code>a2</code>，只使用SDIIS系数</li>
<li>当<code>a2</code>≤ ErrMax≤ <code>a1</code>，根据ErrMax的值按一定的权重来计算</li>
</ul>
<p><code>a1</code>和<code>a2</code>的默认值是<code>0.01</code>和<code>0.0001</code>，收敛比较困难的时候，可以降低阈值，只使用A-DIIS系数</p>
<h3 id="SDIIS"><a href="#SDIIS" class="headerlink" title="SDIIS"></a>SDIIS</h3><p><code>AccelerationMethod SDIIS</code>与在没有指定<code>oldscf</code>的情况下使用<code>NoADIIS</code>意思相同，采用的是阻尼+SDIIS方法。从简单阻尼（Mixing）开始，持续到<code>ErrMax</code>降低到<code>DIIS OK</code>参数以下，但是不超过<code>DIIS Cyc</code>次迭代。之后使用纯的SDIIS方法。</p>
<blockquote>
<p>在指定<code>oldscf</code>的情况下使用<code>NoADIIS</code>会禁用在SCF收敛困难的时候自动切换到ADIIS + SDIIS方法</p>
</blockquote>
<ul>
<li><p>指定<code>NoADIIS</code>时才可以设置 <code>DIIS</code>模块：</p>
<ul>
<li><p><code>N</code>用于加速SCF的扩展向量的数量，默认为<code>10</code>，小于2后会禁用DIIS。此数字不仅适用于Pulay DIIS方案，还适用于其他类似DIIS的方法，例如A-DIIS和LIST方法。</p>
<blockquote>
<p>LIST家族的方法对使用的扩展向量的数量非常敏感，接近收敛时，LIST中的向量数量会增加，但始终受<code>DIIS N</code>的限制，当难以收敛时，可以将<code>DIIS N</code>设置为大于10的值，比如12~20。</p>
</blockquote>
</li>
<li><p><code>OK</code>禁用A-DIIS时，SDIIS起始条件。默认为<code>0.5</code> a.u.。</p>
</li>
<li><p><code>Cyc</code>禁用A-DIIS时，无论上面的<code>DIIS OK</code>值如何，SDIIS都会在此迭代下启动，默认值为<code>5</code></p>
</li>
</ul>
</li>
<li><p><code>Mixing</code>中，指将下一个Fock矩阵确定为$$F = mix F_{n} + (1-mix) F_{n-1}$$，<code>mix</code>默认为<code>0.2</code></p>
</li>
<li><p><code>Mixing1</code>中指第一个SCF循环的混合参数值不同，默认为<code>0.2</code></p>
</li>
</ul>
<h2 id="oldSCF"><a href="#oldSCF" class="headerlink" title="oldSCF"></a>oldSCF</h2><p>在使用<code>Occupations Steep</code>、<code>Lshift</code>，<code>ARH</code>，<code>EDIIS</code>，<code>RESTOCC</code>时，会自动启用<code>oldSCF</code></p>
<h3 id="Lshift"><a href="#Lshift" class="headerlink" title="Lshift"></a>Lshift</h3><p>能级移动方法，在前一次迭代的轨道表示中，Fock矩阵的对角元素提高虚轨道的<code>vshift</code>hartree。旋轨耦合不支持这种方法。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Lshift vshift</span><br><span class="line">Lshift_err Shift_err</span><br><span class="line">Lshift_cyc Shift_cyc</span><br></pre></td></tr></table></figure>

<ul>
<li><code>Lshift_err</code>指定一旦SCF error降至阈值以下，程序将关闭能级移动，<code>Shift_err</code>默认值为<code>0</code>。</li>
<li><code>Lshift_cyc</code>指定在第几个SCF 迭代次数的时候再启动能级移动， <code>Shift_cyc</code>默认为<code>1</code>。</li>
</ul>
<h3 id="ARH"><a href="#ARH" class="headerlink" title="ARH"></a>ARH</h3><figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SCF</span><br><span class="line">  ARH</span><br><span class="line">    &#123;CONV conv&#125;</span><br><span class="line">    &#123;ITER iter&#125;</span><br><span class="line">    &#123;NSAVED nsaved&#125;</span><br><span class="line">    &#123;START start&#125;</span><br><span class="line">    &#123;FINAL&#125; ...</span><br><span class="line">  End</span><br><span class="line">  ...</span><br><span class="line">End</span><br></pre></td></tr></table></figure>

<ul>
<li><code>CONV</code>：当RMS梯度及其最大分量均低于<code> conv</code>，ARH收敛，<code> conv</code>默认是1e-4</li>
<li><code>ITER</code>：最大ARH迭代次数，<code>iter</code>默认为500</li>
<li><code>FINAL</code>：确定ARH完成后是否继续SCF。如果设置了此选项，将执行一次Fock矩阵对角化以获得轨道，并且将停止SCF过程。默认选项为<code>OFF</code>。</li>
<li><code>START</code>：开始调用ARH的SCF迭代次数，<code>start</code>默认为2。使用较大的值可以为ARH提供更好的初猜</li>
<li><code>NSAVED</code>：保存密度和Fock矩阵的数量。 <code>nsaved</code>默认为8。在非常接近费米能级的轨道数很大的收敛困难时，应使用较大的<code>nsaved</code>值。</li>
<li>默认的最小化方法是“Untransformed Pre-conditioned Conjugate Gradient”，可以使用以下两个关键词更改：<ul>
<li><code>NOPRECOND</code>：禁用Untransformed Pre-conditioned CG最小化，不能在第二周期后的元素里使用</li>
<li><code>TRANSPCG</code>：启用Transformed Pre-conditioned CG 方法，SCF可能会更好的收敛。</li>
</ul>
</li>
<li>在每一步SCF中，先执行常规CG最小化，跟踪总步长，如果在某个微迭代中步长超过了信任半径，则过程将在缩小的空间中切换到信任半径优化，在能级移动参数mu收敛后立即中止。在CG最小化期间生成的所有试验矢量的缩小空间中，将最终步骤计算为牛顿步骤。可以通过以下关键词修改参数：<ul>
<li><code>NOSWITCHING</code>：关闭从普通CG到缩小的空间中的信任半径最小化的切换。在某些情况下，使用此选项有助于减少SCF循环的总数。</li>
<li><code>SHIFTED</code>：设置此选项将在第一次微迭代中打开信任半径优化。</li>
<li><code>CGITER=cgiter</code>：设置微迭代的最大数量。</li>
<li>确定置信半径：<ul>
<li><code>TRUSTR=trustr</code>：信任半径的初始值。默认为<code>0.5</code></li>
<li><code>MAXTRUSTR=maxtrustr</code>：最大信任半径值。默认为<code>0.5</code>，并且不会改变。</li>
</ul>
</li>
</ul>
</li>
</ul>
<p>缺点：</p>
<ul>
<li>该方法仅适用于NOSYM计算</li>
<li>在每一步都要计算总能量，计算非常昂贵，而且使用总能量，需要更高的积分精度才能获得稳定的SCF收敛，该方法可能不适用于所有情况，??使用<code>ADDDIFFUSEFIT</code>关键字或更高的<code>ZlmFit</code>质量设置来提高总能量的准确性，从而提高收敛性。??</li>
</ul>
<h1 id="SCF不收敛"><a href="#SCF不收敛" class="headerlink" title="SCF不收敛"></a>SCF不收敛</h1><ol>
<li><p>更换SCF加速收敛的方法</p>
</li>
<li><p>使用ARH方法</p>
</li>
<li><p>修改DIIS算法的参数</p>
<ul>
<li><code>Mixing</code>高于默认值<code>0.2</code>会有更高的加速度，更低会更稳定的迭代</li>
<li><code>N</code>默认为<code>10</code>，越大用于加速SCF的扩展向量的数量越多，SCF迭代更稳定，最多到25</li>
<li><code>Cyc</code>是SDIIS启动的初始SCF迭代步数，默认值为<code>5</code>，更高计算越稳   </li>
</ul>
<p>比如使用如下参数：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">SCF</span><br><span class="line">    DIIS</span><br><span class="line">      N 25</span><br><span class="line">      Cyc 30</span><br><span class="line">    End</span><br><span class="line">    Mixing 0.015</span><br><span class="line">    Mixing1 0.09</span><br><span class="line">  End</span><br></pre></td></tr></table></figure>
</li>
<li><p>HOMO-LUMO较为接近的情况（0.02Hatree以内），可能造成HOMO-LUMO的震荡。可以使用<code>Lshift vshift</code>方法，不适于激发态的计算、解析频率、旋轨耦合的计算。</p>
</li>
<li><p>使用Electron smearing设定在允许的能量范围内分数占据。</p>
</li>
<li><p>其他：<a href="https://www.fermitech.com.cn/wiki/doku.php?id=adf:scfnonconvergence">费米维基</a>的2、3、5。</p>
</li>
</ol>
]]></content>
      <categories>
        <category>计算化学</category>
        <category>ADF</category>
      </categories>
      <tags>
        <tag>SCF</tag>
      </tags>
  </entry>
  <entry>
    <title>搭建个人网站</title>
    <url>/2020/11/06/%E6%90%AD%E5%BB%BA%E7%BD%91%E7%AB%99%E6%95%99%E7%A8%8B/</url>
    <content><![CDATA[<h1 id="搭建"><a href="#搭建" class="headerlink" title="搭建"></a>搭建</h1><h2 id="下载Git和Node-js"><a href="#下载Git和Node-js" class="headerlink" title="下载Git和Node.js"></a>下载Git和Node.js</h2><ol>
<li><p><a href="https://git-scm.com/">Git</a>：安装完成后在Windows PowerShell中输入<code>git --version</code>验证是否安装成功。</p>
</li>
<li><p><a href="https://nodejs.org/en/">Node.js</a>：安装好后在Windows PowerShell输入<code>node -v</code>和<code>npm -v</code>验证是否安装成功。</p>
<h2 id="配置Git"><a href="#配置Git" class="headerlink" title="配置Git"></a>配置Git</h2></li>
<li><p>在windows菜单中，打开<code>Git Bash</code></p>
</li>
<li><p>输入用户名和邮箱</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.name <span class="string">&quot;username&quot;</span></span></span><br><span class="line"><span class="meta">$</span><span class="bash"> git config --global user.email <span class="string">&quot;邮件地址@youremail.com&quot;</span></span></span><br></pre></td></tr></table></figure>
</li>
<li><p>生成新的SSH Key</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> ssh-keygen -t rsa -C <span class="string">&quot;邮件地址@youremail.com&quot;</span></span></span><br></pre></td></tr></table></figure>

<p>一直回车直到出现</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">The key&#x27;s randomart image is:</span><br><span class="line">+---[RSA 3072]----+</span><br><span class="line">|          oo     |</span><br><span class="line">|         . .E    |</span><br><span class="line">|          + o    |</span><br><span class="line">|         * + +   |</span><br><span class="line">|        S B + o  |</span><br><span class="line">|         = + o.O.|</span><br><span class="line">|        + = . OoX|</span><br><span class="line">|       . . +.BoO=|</span><br><span class="line">|        ....+=*=*|</span><br><span class="line">+----[SHA256]-----+</span><br></pre></td></tr></table></figure>
</li>
<li><p>打开github，点击头像→<code>setting</code>→<code>SSH and GPG keys</code>→<code>New SSH key</code></p>
<ul>
<li>输入<code>Title</code></li>
<li>在Git Bash中输入<code>cat ~/.ssh/id_rsa.pub</code>后，复制出现的内容到<code>Key</code>里</li>
<li>点击<code>Add SSH key</code></li>
</ul>
</li>
<li><p>在Git Bash中输入<code>ssh -T git@github.com</code>,再输入<code>yes</code>，出现自己的用户名，表示配置成功</p>
</li>
</ol>
<h2 id="安装Hexo"><a href="#安装Hexo" class="headerlink" title="安装Hexo"></a>安装Hexo</h2><ol>
<li><p>在命令行里利用npm命令安装hexo，最后通过 <code>hexo -v</code>验证是否安装成功。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> npm install -g hexo</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>在Github上创建Repository，Repository  name 中填入<code>username.github.io</code>，点击Create repository</p>
<blockquote>
<p>有很多教程中，在此处可以接着：</p>
<p>在Repository的<code>Setting</code>中的<code>GitHub Pages</code>部分，<code>Choose a theme</code>先选择一个主题，直接可以生成Github主题的网站，再进行后续操作。</p>
<p>但在<a href="https://www.jiqizhixin.com/articles/2020-09-21-10">2020-10-01后，github新建的默认的库位main而不是master</a>，会导致在<code>hexo g -d</code>后，网页一直不更新，此时就需要在<code>GitHub Pages</code>部分的<code>Source</code>中把分支切换为<code>master</code>。</p>
<p>为了避免这个问题，可以跳过这一步；也可以在<code>deploy:</code>中把<code>branch: master</code>改成<code>branch: main</code>。</p>
<img src="1.png" style="zoom: 50%;" />
</blockquote>
</li>
<li><p>在本地创建一个与 Repository 中博客项目同名的文件夹<code>username.github.io</code>后，进入文件夹，右键 <code>Git bash here</code>，输入<code>hexo init</code>初始化文件夹，接着输入<code>npm install</code>安装必备的组件</p>
</li>
<li><p>确保git部署：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
</li>
<li><p>输入<code>hexo g</code>生成静态网页，再输入<code>hexo s</code>打开本地服务器，然后打开浏览器预览：<a href="http://localhost:4000，按`ctrl+c`关闭本地服务器。">http://localhost:4000，按`ctrl+c`关闭本地服务器。</a></p>
</li>
</ol>
<h2 id="将博客部署到username-github-io"><a href="#将博客部署到username-github-io" class="headerlink" title="将博客部署到username.github.io"></a>将博客部署到username.github.io</h2><ol>
<li><p>进入 Github 的网站 Repository，点击<code>code</code>→<code>SSH</code>，复制</p>
</li>
<li><p>打开网站文件夹根目录中的~/_config.yml<code>站点配置文件</code>，将<code>deploy</code>部分修改为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repository: git@github.com:username&#x2F;username.github.io.git</span><br><span class="line">  branch: master</span><br></pre></td></tr></table></figure>

<p>同时可以顺便修改<code>site</code>部分:</p>
<ul>
<li><code>title</code>博客名</li>
<li><code>subtitle</code>副标题</li>
<li><code>description</code>对网站的描述，可以自定义，给搜索引擎看</li>
<li><code>author</code>作者，在博客底部可以看到</li>
<li><code>email</code>联系邮箱</li>
<li><code>language</code>默认英文，中文为<code>zh-CN</code></li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">title: Hexo</span><br><span class="line">subtitle: &#39;&#39;</span><br><span class="line">description: &#39;&#39;</span><br><span class="line">keywords:</span><br><span class="line">author: John Doe</span><br><span class="line">email:</span><br><span class="line">language: en</span><br><span class="line">timezone: &#39;&#39;</span><br></pre></td></tr></table></figure>
</li>
<li><p><u><strong>以后每次修改本地文件后，都需要输入：</strong></u></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo g</span><br><span class="line">hexo s	#预览</span><br><span class="line">hexo d</span><br></pre></td></tr></table></figure>

<p>也可以不预览，直接<code>hexo g -d</code>来更新网站。再打开网站就可以了。如果修改了内容，<code>hexo g -d</code>后原网页没有反应，可以用<code>ctrl+F5</code>强制刷新</p>
</li>
</ol>
<h2 id="在多台电脑上操作"><a href="#在多台电脑上操作" class="headerlink" title="在多台电脑上操作"></a>在多台电脑上操作</h2><p>原理：</p>
<p><code>hexo g -d</code>完成的是将在<del>/public文件夹下生成静态的网页文件，然后将其复制到</del>/.deploy_git文件夹里，再通过Github push到创建的网页分支<code>master</code>上。</p>
<p>其他文件夹hexo的模块<code>node_modules</code>，文章文件<code>source</code>，主题文件<code>themes</code>等等都没有上传到<code>master</code>上，因此可以通过再创建一个分支来进行同步。</p>
<ol>
<li><p>首先在Github的 <code>username.github.io</code> Repository中点击<code>master</code>按钮，输入<code>source</code>并回车，新建一个同步所有文件的分支<code>source</code></p>
</li>
<li><p>再通过<code>branches</code>→<code>All branches</code>→<code>change default branch</code>，切换为source后<code>updata</code>，回到 <code>username.github.io</code> Repository中，在<code>code</code>中复制<code>SSH</code></p>
</li>
<li><p>新建一个空文件夹XXXX，在此文件夹下右键 <code>Git bash here</code>，输入：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ git clone 复制的ssh</span><br></pre></td></tr></table></figure>
</li>
<li><p>进入XXXX/username.github.io文件夹，将该文件夹下除了.git（默认是隐藏文件）的文件夹全部删除</p>
</li>
<li><p>将本来网站的目录下username.github.io除了.deploy_git的文件全部复制到XXXX/username.github.io文件夹，其中有一个<code>.gitignore</code>文件，表示其中的文件不需要上传。另外，因为一个git仓库中不能包含另一个git仓库，如果在theme下git clone了主题文件，需要把相应目录下的.git文件夹删除。</p>
</li>
<li><p>在命令行下，<code>cd username.github.io</code>进入该文件夹下，可以看到最后有个<code>(source)</code>表明当前在source分支下：</p>
<ul>
<li><code>git add .</code>告诉Git，把文件添加到仓库</li>
<li><code>git commit</code>告诉Git，把文件提交到仓库，-m后面是修改的说明</li>
<li><code>git push</code> 将本地分支的更新，推送到远程</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit –m &quot;add branch&quot;</span><br><span class="line">git push </span><br></pre></td></tr></table></figure>
</li>
<li><p>在其他电脑上的操作如下：</p>
<ol>
<li><p>准备工作：在该电脑上需要准备好Git和Node等，配置好<code>SSH key</code>，安装hexo等等</p>
</li>
<li><p>新建文件夹YYYYYYY，然后再该文件夹下 <code>git clone 复制的ssh</code>，cd 进入<code>username.github.io</code> 文件夹下，执行<code>npm install</code>下载必要的组件，也会有一个<code>.gitignore</code>文件</p>
</li>
<li><p>在该文件夹下修改后，<code>hexo g -d</code>更新网站。</p>
</li>
<li><p>然后将更新推送到<code>source</code>分支</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git add .</span><br><span class="line">git commit –m &quot;add branch&quot;</span><br><span class="line">git push </span><br></pre></td></tr></table></figure>
</li>
<li><p>以后不管在原本电脑还是其他电脑上，首先<code>git pull</code> 获取更新，然后再进行修改，再推送到<code>source</code>分支。</p>
</li>
</ol>
</li>
</ol>
<h1 id="文章相关"><a href="#文章相关" class="headerlink" title="文章相关"></a>文章相关</h1><h2 id="新建文章"><a href="#新建文章" class="headerlink" title="新建文章"></a>新建文章</h2><ol>
<li><p>在根目录下右键并GIt Bash here，然后输入<code>hexo new post &quot;article title&quot;</code>，新建一篇文章，此时在~/source_posts下就出现了<code>article title.md</code>文件，进行编辑即可。</p>
</li>
<li><p>为了新建文章方便，可以将~/scaffolds/post.md的Front-matter部分修改为自己默认需要的：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: &#123;&#123; title &#125;&#125; </span><br><span class="line">date: &#123;&#123; date &#125;&#125; </span><br><span class="line">updated: </span><br><span class="line">tags: </span><br><span class="line">categories: </span><br><span class="line">typora-root-url: &#123;&#123; title &#125;&#125; </span><br><span class="line">---</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="在文章中插入图片"><a href="#在文章中插入图片" class="headerlink" title="在文章中插入图片"></a>在文章中插入图片</h2><ol>
<li>如果只有少量图片，可以在~/source目录下新建images文件夹，通过<code>![](/images/xxx.png)</code>来引用。</li>
<li>如果图片比较多，可以建立文章自己的目录，在<code>站点配置文件</code>中，将<code>post_asset_folder</code>关键词改成<code>true</code>。之后通过<code>hexo new post &quot;article title&quot;</code>在新建文章时，就会出现与文章名相同的文件夹<code>article title</code>，将图片放入其中后，通过<code>![](XXXX.jpg)</code>直接引用。</li>
<li>基于md编辑器Typora编辑：<ul>
<li>以上在Typora中无法显示图片，可以在md文件的Front-matter部分，根据文章所有图片存在的位置，添加<code>typora-root-url: ..</code>或者<code>typora-root-url: article title</code>，即可在编辑器中也正常显示图片。</li>
<li>在Typora的偏好设置→图像部分→选择复制到指定路径， <code>./$&#123;filename&#125;</code>，之后可以直接在编辑md文件的时候复制图片，文章中会显示图像，并且已经在<code>article title</code>的文件夹下复制了该图片，可以用第二种方法引用。</li>
<li>Typora编辑器可以直接在图片上的代码右键缩放图片，此时代码会相应的更改为<code>&lt;img src=&quot;1.png&quot; style=&quot;zoom: 50%;&quot; /&gt;</code>，网页上也能成功缩小。</li>
</ul>
</li>
</ol>
<h2 id="创建分类"><a href="#创建分类" class="headerlink" title="创建分类"></a>创建分类</h2><ol>
<li><p>在根目录下右键并GIt Bash here，然后输入<code>hexo new page categories</code>，在<code>~/source/categories/</code>出现了<code>index.md</code>，打开在Front-matter部分增加<code>type: &quot;categories&quot;</code>    </p>
</li>
<li><p>在文章中的ront-matter中的categories后增加相应分类即可</p>
<ol>
<li><p>如果同一篇文章需要放到子分类下，可以用两种方法放到<code>AAA/BBB</code>分类下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">categories:</span><br><span class="line">  - AAA</span><br><span class="line">  - BBB</span><br></pre></td></tr></table></figure>

<p>或</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">categories: [AAA,BBB]</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果同一篇文章需要放到两个分类下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">categories:</span><br><span class="line">  - [AAA]</span><br><span class="line">  - [BBB]</span><br></pre></td></tr></table></figure>


</li>
</ol>
</li>
</ol>
<h2 id="创建标签"><a href="#创建标签" class="headerlink" title="创建标签"></a>创建标签</h2><ol>
<li><p>在根目录下右键并GIt Bash here，然后输入<code>hexo new page tags</code>，在<code>~/source/tags/</code>出现了<code>index.md</code>，打开在Front-matter部分增加<code>type: &quot;categories&quot;</code>    </p>
</li>
<li><p>在文章中的tags后增加相应分类即可</p>
<ul>
<li>如果同一篇文章需要多个标签，可以使用<code>tags: [标签1,标签2,标签3]</code></li>
</ul>
</li>
</ol>
<h2 id="设置页面文章篇数"><a href="#设置页面文章篇数" class="headerlink" title="设置页面文章篇数"></a>设置页面文章篇数</h2><ol>
<li><p>使用 <code>npm install --save</code> 命令来安装需要的 Hexo 插件。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install --save hexo-generator-index</span><br><span class="line">npm install --save hexo-generator-archive</span><br><span class="line">npm install --save hexo-generator-tag</span><br></pre></td></tr></table></figure>
</li>
<li><p>在 <code>站点配置文件</code> 中：</p>
<ul>
<li>per_page 即文章的数量</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">index_generator:</span><br><span class="line">  per_page: 5  </span><br><span class="line">archive_generator:</span><br><span class="line">  per_page: 20</span><br><span class="line">  yearly: true</span><br><span class="line">  monthly: true</span><br><span class="line">tag_generator:</span><br><span class="line">  per_page: 10</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h1 id="基于Next-v7-8-0的构建"><a href="#基于Next-v7-8-0的构建" class="headerlink" title="基于Next v7.8.0的构建"></a>基于<a href="https://github.com/theme-next/hexo-theme-next">Next v7.8.0</a>的构建</h1><h2 id="修改主题"><a href="#修改主题" class="headerlink" title="修改主题"></a>修改主题</h2><ol>
<li><p>选择主题的github项目，然后点击Cod→SSH，复制后，在~/themes文件夹下右键，GIt Bash here，然后输入：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git clone git@github.com:theme-next/hexo-theme-next.git</span><br></pre></td></tr></table></figure>
</li>
<li><p>打开<code>站点配置文件</code>，将theme后面修改为该主题的名字</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">theme: hexo-theme-next</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>主题配置文件</code>~/themes/hexo-theme-next/_config.yml中，在<code>Schemes</code>部分可以选择不同的风格。还可以通过<code>darkmode</code>开启黑暗模式</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">#scheme: Muse</span><br><span class="line">#scheme: Mist</span><br><span class="line">#scheme: Pisces</span><br><span class="line">scheme: Gemini</span><br><span class="line"></span><br><span class="line">darkmode: false</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="修改菜单"><a href="#修改菜单" class="headerlink" title="修改菜单"></a>修改菜单</h2><p>在<code>主题配置文件</code>的<code>Menu Settings</code>部分：</p>
<ul>
<li>设定菜单的内容，格式为<code>Key: /link/ || icon</code><ul>
<li><code>key</code>如果如果有默认翻译，会自动加载翻译，如果不存在就不会加载。翻译可以在<code>\themes\hexo-theme-next\languages</code>的<code>.yml</code>文件中修改。</li>
<li><code>||</code>前面是菜单的链接，后面是菜单的Font Awesome图标。</li>
</ul>
</li>
<li>可以通过修改<code>icons</code>是否显示菜单的图标</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">menu:</span><br><span class="line">  home: &#x2F; || fa fa-home</span><br><span class="line">  #about: &#x2F;about&#x2F; || fa fa-user</span><br><span class="line">  #tags: &#x2F;tags&#x2F; || fa fa-tags</span><br><span class="line">  #categories: &#x2F;categories&#x2F; || fa fa-th</span><br><span class="line">  archives: &#x2F;archives&#x2F; || fa fa-archive</span><br><span class="line">  #schedule: &#x2F;schedule&#x2F; || fa fa-calendar</span><br><span class="line">  #sitemap: &#x2F;sitemap.xml || fa fa-sitemap</span><br><span class="line">  #commonweal: &#x2F;404&#x2F; || fa fa-heartbeat</span><br><span class="line"></span><br><span class="line"># Enable &#x2F; Disable menu icons &#x2F; item badges.</span><br><span class="line">menu_settings:</span><br><span class="line">  icons: true</span><br><span class="line">  badges: false</span><br></pre></td></tr></table></figure>

<h2 id="设置字体及其大小等"><a href="#设置字体及其大小等" class="headerlink" title="设置字体及其大小等"></a>设置字体及其大小等</h2><h3 id="修改英文字体"><a href="#修改英文字体" class="headerlink" title="修改英文字体"></a>修改英文字体</h3><p>在<code>主题配置文件</code>中的<code>font</code>部分，有以下几种：</p>
<ul>
<li><code>global</code>全局字体：定义的字体将在全站范围使用</li>
<li><code>title</code>网站标题字体</li>
<li><code>headings</code>标题字体：文章内标题的字体（h1, h2, h3, h4, h5, h6）</li>
<li><code>posts</code>文章字体</li>
<li><code>codes</code>代码字体</li>
</ul>
<ol>
<li>每一项都有一个额外的 <code>external</code> 属性，此属性用来控制是否使用外链字体库。 </li>
<li>通过<code>family</code>设置字体，直接填写外链字体库的名字即可使用，但是Pisces / Gemini两种主题的<code>global</code>和<code>title</code>只能选择给定的字体。</li>
<li><code>size</code>设置字体大小，默认是<code>1</code>em(16px)。在此处的设置最好只设置英文字体，如果设置了中文字体，则会导致英文使用的是中文自带的字体。</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">font:</span><br><span class="line">  enable: false</span><br><span class="line"></span><br><span class="line">  # Uri of fonts host, e.g. https:&#x2F;&#x2F;fonts.googleapis.com (Default).</span><br><span class="line">  host:</span><br><span class="line"></span><br><span class="line">  global:</span><br><span class="line">    external: true</span><br><span class="line">    family: Lato</span><br><span class="line">    size:</span><br><span class="line">  title:</span><br><span class="line">    external: true</span><br><span class="line">    family:</span><br><span class="line">    size:</span><br><span class="line">  headings:</span><br><span class="line">    external: true</span><br><span class="line">    family:</span><br><span class="line">    size:</span><br><span class="line">  posts:</span><br><span class="line">    external: true</span><br><span class="line">    family:</span><br><span class="line">  codes:</span><br><span class="line">    external: true</span><br><span class="line">    family:</span><br></pre></td></tr></table></figure>

<h3 id="修改中文字体"><a href="#修改中文字体" class="headerlink" title="修改中文字体"></a>修改中文字体</h3><ol>
<li><p>修改中文字体，以思源宋体为例，首先打开<code>~\themes\hexo-theme-next\source\css\_variables\base.styl</code>，找到<code>// Font families.</code>模块，修改变量<code>$font-family-chinese</code>的值：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; Font families.</span><br><span class="line">$font-family-chinese      &#x3D; &quot;Noto Serif SC&quot;;</span><br></pre></td></tr></table></figure>

<p>同时也可以在<code>// Font size</code>部分修改字体大小，行高</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#x2F;&#x2F; Font size</span><br><span class="line">$font-size-base           &#x3D; (hexo-config(&#39;font.enable&#39;) and hexo-config(&#39;font.global.size&#39;) is a &#39;unit&#39;) ? unit(hexo-config(&#39;font.global.size&#39;), em) : 1em;</span><br><span class="line"></span><br><span class="line">&#x2F;&#x2F; Global line height</span><br><span class="line">$line-height-base         &#x3D; 2;</span><br><span class="line">$line-height-code-block   &#x3D; 1.6; &#x2F;&#x2F; Can&#39;t be less than 1.3;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<a href="https://fonts.google.com/">Google外链字体库</a>中，搜索并打开Noto Serif SC的页面，通过<code>Select this style</code>选择想要的样式，再点击右上角<code>View your selected families</code>，复制生成的 <code>&lt;link&gt;</code> 代码到 ~/source/_data/head.swig 文件中，并将<code>主题配置文件</code>中<code>custom_file_path:</code>部分的#去掉。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">custom_file_path:</span><br><span class="line">  head: source&#x2F;_data&#x2F;head.swig</span><br><span class="line">  #header: source&#x2F;_data&#x2F;header.swig</span><br><span class="line">  sidebar: source&#x2F;_data&#x2F;sidebar.swig</span><br><span class="line">  #postMeta: source&#x2F;_data&#x2F;post-meta.swig</span><br><span class="line">  #postBodyEnd: source&#x2F;_data&#x2F;post-body-end.swig</span><br><span class="line">  #footer: source&#x2F;_data&#x2F;footer.swig</span><br><span class="line">  #bodyEnd: source&#x2F;_data&#x2F;body-end.swig</span><br><span class="line">  #variable: source&#x2F;_data&#x2F;variables.styl</span><br><span class="line">  #mixin: source&#x2F;_data&#x2F;mixins.styl</span><br><span class="line">  style: source&#x2F;_data&#x2F;styles.styl</span><br></pre></td></tr></table></figure>

</li>
</ol>
<blockquote>
<p><strong>Next自定义样式放在了 ~/source/_data/目录下，在自定义样式之后，需要在<code>custom_file_path:</code>中启用相应的路径。</strong></p>
</blockquote>
<h3 id="修改行间代码字体的大小"><a href="#修改行间代码字体的大小" class="headerlink" title="修改行间代码字体的大小"></a>修改行间代码字体的大小</h3><p>在~/source/_data/styles.styl文件中增加：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">code &#123;</span><br><span class="line">	margin: 0px 4px;</span><br><span class="line">	font-size: 80%;                                                        </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>

<h2 id="侧栏"><a href="#侧栏" class="headerlink" title="侧栏"></a>侧栏</h2><h3 id="修改侧边栏的位置和出现时机"><a href="#修改侧边栏的位置和出现时机" class="headerlink" title="修改侧边栏的位置和出现时机"></a>修改侧边栏的位置和出现时机</h3><p>在<code>主题配置文件</code>的修改<code>sidebar</code>：</p>
<ul>
<li><code>position</code>设置侧边栏在左边<code>left</code>还是右边<code>right</code></li>
<li><code>width</code>设置侧边栏的宽度，默认Muse和Mist是320,Pisces和Gemini是240，可以不设置。</li>
<li><code>display</code>用于设置侧边栏的显示方式：<ul>
<li><code>post</code> 在文章页面（拥有目录列表）时显示</li>
<li><code>always</code>  在所有页面中都显示</li>
<li><code>hide</code> 在所有页面中都隐藏（可以手动展开）</li>
<li><code>remove</code> 完全移除</li>
</ul>
</li>
<li><code>offset</code>设置侧边栏距离顶部介绍的距离，只对<code>Muse</code>和<code>Mist</code>两个主题起作用</li>
<li><code>onmobile</code>设置是否在手机等宽度较小的设备上显示侧边栏，只对<code>Muse</code>和<code>Mist</code>两个主题起作用</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sidebar:</span><br><span class="line">  position: left</span><br><span class="line">  #width: 300</span><br><span class="line">  display: post</span><br><span class="line"></span><br><span class="line">  # Sidebar padding in pixels.</span><br><span class="line">  padding: 18</span><br><span class="line">  </span><br><span class="line">  offset: 12</span><br><span class="line">  onmobile: false</span><br></pre></td></tr></table></figure>

<h3 id="设置头像"><a href="#设置头像" class="headerlink" title="设置头像"></a>设置头像</h3><p>修改<code>主题配置文件</code>中的<code>avatar</code>部分：</p>
<ul>
<li><code>url</code>中可以是完整的互联网 URI，也可以将头像放置在<code>source/images/</code> 目录下</li>
<li>修改<code>rounded</code>，头像可以为圆的</li>
<li>修改<code>rotated</code>，鼠标移动到头像上头像会旋转</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">avatar:</span><br><span class="line">  url: #&#x2F;images&#x2F;avatar.gif</span><br><span class="line">  rounded: false</span><br><span class="line">  rotated: false</span><br></pre></td></tr></table></figure>
<h3 id="社交链接"><a href="#社交链接" class="headerlink" title="社交链接"></a>社交链接</h3><p>修改<code>主题配置文件</code>中的<code>social</code>部分：</p>
<ul>
<li><code>||</code>前输入你社交主页的链接即可，后面用于设置图标</li>
<li>email中的mailto注意不要删去，这样在win10系统可以调用系统邮件服务自动填写收件人</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">social:</span><br><span class="line">  #GitHub: https:&#x2F;&#x2F;github.com&#x2F;yourname || fab fa-github</span><br><span class="line">  #E-Mail: mailto:yourname@gmail.com || fa fa-envelope</span><br><span class="line">  #Weibo: https:&#x2F;&#x2F;weibo.com&#x2F;yourname || fab fa-weibo</span><br><span class="line">  #Google: https:&#x2F;&#x2F;plus.google.com&#x2F;yourname || fab fa-google</span><br><span class="line">  #Twitter: https:&#x2F;&#x2F;twitter.com&#x2F;yourname || fab fa-twitter</span><br><span class="line">  #FB Page: https:&#x2F;&#x2F;www.facebook.com&#x2F;yourname || fab fa-facebook</span><br><span class="line">  #StackOverflow: https:&#x2F;&#x2F;stackoverflow.com&#x2F;yourname || fab fa-stack-overflow</span><br><span class="line">  #YouTube: https:&#x2F;&#x2F;youtube.com&#x2F;yourname || fab fa-youtube</span><br><span class="line">  #Instagram: https:&#x2F;&#x2F;instagram.com&#x2F;yourname || fab fa-instagram</span><br><span class="line">  #Skype: skype:yourname?call|chat || fab fa-skype</span><br><span class="line">social_icons:</span><br><span class="line">  enable: true</span><br><span class="line">  icons_only: false</span><br><span class="line">  transition: false</span><br></pre></td></tr></table></figure>

<h3 id="侧边栏目录"><a href="#侧边栏目录" class="headerlink" title="侧边栏目录"></a>侧边栏目录</h3><p>修改<code>主题配置文件</code>中的<code>toc</code>部分：</p>
<ul>
<li><code>enable</code>用于设置是否开启侧边栏目录</li>
<li><code>number</code>用于设置自动编号</li>
<li><code>wrap</code>用于设置当标题长度超过侧边栏长度时是否自动换行</li>
<li><code>expand_all</code>用于设置是否自动展开全部目录</li>
<li><code>max_depth</code>用于设置自动生成目录的最大深度</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">toc:</span><br><span class="line">  enable: true</span><br><span class="line">  number: true</span><br><span class="line">  wrap: false</span><br><span class="line">  expand_all: false</span><br><span class="line">  max_depth: 6</span><br></pre></td></tr></table></figure>

<h3 id="标签云"><a href="#标签云" class="headerlink" title="标签云"></a>标签云</h3><ol>
<li><p>安装插件<code>npm install hexo-tag-cloud@^2.0.* --save</code></p>
</li>
<li><p>在 <code>hexo/source/_data</code> 目录下新建 <code>sidebar.swig</code> 文件，输入</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;% if site.tags.length &gt; 1 %&#125;</span><br><span class="line">&lt;script type&#x3D;&quot;text&#x2F;javascript&quot; charset&#x3D;&quot;utf-8&quot; src&#x3D;&quot;&#x2F;js&#x2F;tagcloud.js&quot;&gt;&lt;&#x2F;script&gt;</span><br><span class="line">&lt;script type&#x3D;&quot;text&#x2F;javascript&quot; charset&#x3D;&quot;utf-8&quot; src&#x3D;&quot;&#x2F;js&#x2F;tagcanvas.js&quot;&gt;&lt;&#x2F;script&gt;</span><br><span class="line">&lt;div class&#x3D;&quot;widget-wrap&quot;&gt;</span><br><span class="line">    &lt;h3 class&#x3D;&quot;widget-title&quot;&gt;标签云&lt;&#x2F;h3&gt;</span><br><span class="line">    &lt;div id&#x3D;&quot;myCanvasContainer&quot; class&#x3D;&quot;widget tagcloud&quot;&gt;</span><br><span class="line">        &lt;canvas width&#x3D;&quot;250&quot; height&#x3D;&quot;250&quot; id&#x3D;&quot;resCanvas&quot; style&#x3D;&quot;width&#x3D;100%&quot;&gt;</span><br><span class="line">            &#123;&#123; list_tags() &#125;&#125;</span><br><span class="line">        &lt;&#x2F;canvas&gt;</span><br><span class="line">    &lt;&#x2F;div&gt;</span><br><span class="line">&lt;&#x2F;div&gt;</span><br><span class="line">&#123;% endif %&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>主题配置文件</code>中的<code>custom_file_path</code>部分，增加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">sidebar: source&#x2F;_data&#x2F;sidebar.swig</span><br></pre></td></tr></table></figure>

<p>并在<code>主题配置文件</code>中其中添加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 标签云</span><br><span class="line">tag_cloud:</span><br><span class="line">    textFont: Trebuchet MS, Helvetica</span><br><span class="line">    textColor: &#39;#333&#39;</span><br><span class="line">    textHeight: 25</span><br><span class="line">    outlineColor: &#39;#E2E1D1&#39;</span><br><span class="line">    maxSpeed: 0.1</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="设置网站访问量"><a href="#设置网站访问量" class="headerlink" title="设置网站访问量"></a>设置网站访问量</h2><ol>
<li><p>登陆<a href="https://tongji.baidu.com/web/welcome/login">百度统计</a>，<code>新增网站</code>，<code>网站域名</code>和<code>网站首页</code>都填网站的地址即可。</p>
</li>
<li><p>在 <code>hm.src</code>中，复制 <code>hm.js?</code> 后面那串统计脚本 id到<code>主题配置文件</code>中<code>baidu_analytics</code>后面</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="javascript"><span class="keyword">var</span> _hmt = _hmt || [];</span></span><br><span class="line"><span class="javascript">(<span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</span></span><br><span class="line"><span class="javascript">  <span class="keyword">var</span> hm = <span class="built_in">document</span>.createElement(<span class="string">&quot;script&quot;</span>);</span></span><br><span class="line"><span class="javascript">  hm.src = <span class="string">&quot;//hm.baidu.com/hm.js?xxxxxxxxxxxxxxxxxxx&quot;</span>;</span></span><br><span class="line"><span class="javascript">  <span class="keyword">var</span> s = <span class="built_in">document</span>.getElementsByTagName(<span class="string">&quot;script&quot;</span>)[<span class="number">0</span>]; </span></span><br><span class="line">  s.parentNode.insertBefore(hm, s);</span><br><span class="line">&#125;)();</span><br><span class="line"><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>修改完参数后执行 <code>hexo g</code> 和 <code>hexo d</code> 命令部署博客。在百度统计页面，点击<code>代码检查</code>，当出现”页面代码安装状态：代码安装正确“时，表示配置成功。</p>
</li>
<li><p>在<code>主题配置文件</code>中，将<code>enable</code>修改为<code>true</code>，就可以在页面添加统计模块。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">busuanzi_count:</span><br><span class="line">  enable: true</span><br><span class="line">  total_visitors: true</span><br><span class="line">  total_visitors_icon: fa fa-user</span><br><span class="line">  total_views: true</span><br><span class="line">  total_views_icon: fa fa-eye</span><br><span class="line">  post_views: true</span><br><span class="line">  post_views_icon: fa fa-eye</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="增加搜索功能"><a href="#增加搜索功能" class="headerlink" title="增加搜索功能"></a>增加搜索功能</h2><ol>
<li><p>在根目录下右键并Git Bash here，然后输入</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-searchdb --save</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>站点配置</code>文件中添加搜索设置</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">search:</span><br><span class="line">  path: search.xml</span><br><span class="line">  field: post</span><br><span class="line">  format: html</span><br><span class="line">  limit: 10000</span><br></pre></td></tr></table></figure>
</li>
<li><p>将<code>主题配置文件</code>中的<code>Local Search</code>的<code>enable</code>修改为<code>true</code>：</p>
<ul>
<li><code>top_n_per_article</code>表示每篇文章出现几条匹配，<code>-1</code>代表全部</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">local_search:</span><br><span class="line">  enable: ture</span><br><span class="line">  trigger: auto</span><br><span class="line">  top_n_per_article: 1</span><br><span class="line">  unescape: false</span><br><span class="line">  preload: false</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h2 id="文章相关-1"><a href="#文章相关-1" class="headerlink" title="文章相关"></a>文章相关</h2><h3 id="增加字数统计"><a href="#增加字数统计" class="headerlink" title="增加字数统计"></a>增加字数统计</h3><ol>
<li><p>安装插件<code>npm install hexo-symbols-count-time --save</code></p>
</li>
<li><p>在<code>站点配置文件</code>中增加：</p>
<ul>
<li><code>symbols</code>：文章字数</li>
<li><code>time</code>：阅读时长</li>
<li><code>total_symbols</code>：网页底部是否显示所有文章总字数</li>
<li><code>total_time</code>：网页底部所有文章阅读中时长</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">symbols_count_time:</span><br><span class="line">  symbols: true </span><br><span class="line">  time: true </span><br><span class="line">  total_symbols: true </span><br><span class="line">  total_time: true </span><br></pre></td></tr></table></figure>
</li>
<li><p>修改<code>主题配置文件</code>中的 <code>symbols_count_time</code> 部分：</p>
<ul>
<li><code>separated_meta</code>：统计信息不换行显示</li>
<li><code>item_text_post</code>：文章统计信息中是否显示“本文字数/阅读时长”等描述文字</li>
<li><code>item_text_total</code>：底部footer站点统计信息中是否显示“本文字数/阅读时长”等描述文字</li>
<li><code>awl</code>：平均字符长度</li>
<li><code>wpm</code>： 阅读速度, 一分钟阅读的字数</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">symbols_count_time:</span><br><span class="line">  separated_meta: true </span><br><span class="line">  item_text_post: true </span><br><span class="line">  item_text_total: true </span><br><span class="line">  awl: 4 </span><br><span class="line">  wpm: 275 </span><br></pre></td></tr></table></figure>


</li>
</ol>
<h3 id="图片的放大功能"><a href="#图片的放大功能" class="headerlink" title="图片的放大功能"></a>图片的放大功能</h3><p>将<code>主题配置文件</code>中的<code>fancybox</code>关键词为<code>true</code>即可，此时点击图片会有浮动的放大功能。</p>
<h3 id="显示公式"><a href="#显示公式" class="headerlink" title="显示公式"></a>显示公式</h3><p>在<code>主题配置文件</code>中的<code>math</code>部分：</p>
<ul>
<li><code>per_pag</code>中，<code>true</code>代表按需要加载公式，在.md文件Front-matter添加<code>mathjax: true</code>才会加载公式。否则在所有页面都加载</li>
<li><code>mathjax</code>的enable改为<code>true</code></li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">math:</span><br><span class="line">  per_page: true</span><br><span class="line">  mathjax:</span><br><span class="line">    enable: true</span><br><span class="line">    mhchem: false</span><br><span class="line">  katex:</span><br><span class="line">    enable: false</span><br><span class="line">    copy_tex: false</span><br></pre></td></tr></table></figure>

<h3 id="主页不显示全文"><a href="#主页不显示全文" class="headerlink" title="主页不显示全文"></a>主页不显示全文</h3><ol>
<li>在文章中使用 <code>&lt;!-- more --&gt;</code> 手动进行截断</li>
<li>在文章的 front-matter 中添加 description，并提供文章摘录</li>
</ol>
<h3 id="打赏功能"><a href="#打赏功能" class="headerlink" title="打赏功能"></a>打赏功能</h3><ol>
<li><p>将支付二维码放置在<code>source/images/</code> 目录下</p>
</li>
<li><p>修改<code>主题配置文件</code>中的<code>reward_settings</code>部分</p>
<ul>
<li><code>enable</code>用于设置是否在文章末尾显示打赏按钮</li>
<li><code>animation</code>开启动画后，XX支付的字会抖动</li>
<li><code>comment</code>在打赏按钮的上方出现的字</li>
<li><code>reward</code>放置二维码的路径</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">reward_settings:</span><br><span class="line">  enable: false</span><br><span class="line">  animation: false</span><br><span class="line">  #comment: Donate comment here.</span><br><span class="line">reward:</span><br><span class="line">  wechatpay: &#x2F;images&#x2F;wechatpay.png</span><br><span class="line">  #alipay: &#x2F;images&#x2F;alipay.png</span><br><span class="line">  #paypal: &#x2F;images&#x2F;paypal.png</span><br><span class="line">  #bitcoin: &#x2F;images&#x2F;bitcoin.png</span><br></pre></td></tr></table></figure>

</li>
</ol>
<h3 id="评论"><a href="#评论" class="headerlink" title="评论"></a>评论</h3><ol>
<li>注册并登陆<a href="https://www.leancloud.cn/">LeanCloud</a>，<code>创建应用</code>，输入应用名称后创建</li>
<li>点击<code>齿轮图标</code>→<code>应用Key</code>，复制<code>AppID</code>，<code>AppKey</code>，在<code>主题配置文件</code>的<code>valine</code>部分修改：<ul>
<li><code>enable</code>改为 <code>true</code></li>
<li><code>appid</code>和 <code>appkey</code>粘贴在lean复制的App ID和App Key</li>
<li><code>notify</code>新留言是否需要通知</li>
<li><code>verify</code>验证码</li>
<li> <code>placeholder</code>默认留言框内的文字</li>
<li> <code>avatar</code>默认头像</li>
<li><code>guest_info</code>默认留言框的头部需要访问者输入的信息</li>
<li><code>pageSize</code>默认单页的留言条数</li>
<li><code>language</code>语言，可改为<code>en</code>, <code>zh-cn</code></li>
<li><code>visitor</code>开启后可能会与百度网站访问量的模块冲突，显示出两个阅读次数，因此可以关闭。</li>
<li><code> comment_count</code>评论次数</li>
</ul>
</li>
</ol>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">valine:</span><br><span class="line">  enable: true</span><br><span class="line">  appid: XXXXXXXXX</span><br><span class="line">  appkey: XXXXXXXX</span><br><span class="line">  notify: false </span><br><span class="line">  verify: false </span><br><span class="line">  placeholder: Just go go </span><br><span class="line">  avatar: mm # Gravatar style</span><br><span class="line">  guest_info: nick,mail,link </span><br><span class="line">  pageSize: 10 </span><br><span class="line">  language: </span><br><span class="line">  visitor: false</span><br><span class="line">  comment_count: true </span><br><span class="line">  recordIP: false # Whether to record the commenter IP</span><br><span class="line">  serverURLs: # When the custom domain name is enabled, fill it in here (it will be detected automatically by default, no need to fill in)</span><br><span class="line">  #post_meta_order: 0</span><br></pre></td></tr></table></figure>

<ol start="3">
<li>可以在LeanCloud中的<code>储存</code>→<code>结构化数据</code>→<code>Comment</code>中管理评论</li>
<li>开启后，<code>tag</code>、<code>categories</code>页面也会带有评论， 若需要关闭的话，可以在不想要评论页面下相应的md文件的Front-matter部分添加字段 <code>comments: false</code> 。</li>
</ol>
<h3 id="设置代码高亮主题和复制"><a href="#设置代码高亮主题和复制" class="headerlink" title="设置代码高亮主题和复制"></a>设置代码高亮主题和复制</h3><p>在<code>主题配置文件</code>的<code>codeblock</code>部分修改：</p>
<ul>
<li><code>highlight_theme</code>设置代码高亮格式，有<code>normal</code>| <code>night</code> | <code>night eighties</code> | night blue | <code>night bright</code> | <code>solarized</code> | <code>solarized dark</code> | <code>galactic</code>可选</li>
<li><code>copy_button</code>开启代码块右上角的复制按钮，<code>show_result</code>设置是否显示复制成功提示</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">codeblock:</span><br><span class="line">  highlight_theme: normal</span><br><span class="line">  copy_button:</span><br><span class="line">    enable: true</span><br><span class="line">    show_result: false</span><br><span class="line">    # Available values: default | flat | mac</span><br><span class="line">    style:</span><br></pre></td></tr></table></figure>



<h3 id="页面加载进度和阅读进度"><a href="#页面加载进度和阅读进度" class="headerlink" title="页面加载进度和阅读进度"></a>页面加载进度和阅读进度</h3><p><code>主题配置文件</code>中<code>reading_progress</code>：</p>
<ul>
<li><code>position</code>位置是top还是bottom</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">reading_progress:</span><br><span class="line">  enable: false</span><br><span class="line">  position: top</span><br><span class="line">  color: &quot;#37c6c0&quot;</span><br><span class="line">  height: 3px</span><br></pre></td></tr></table></figure>

<h3 id="通过一个按钮回到网页顶部"><a href="#通过一个按钮回到网页顶部" class="headerlink" title="通过一个按钮回到网页顶部"></a>通过一个按钮回到网页顶部</h3><p><code>主题配置文件</code>中的<code>back2top</code>部分：</p>
<ul>
<li><p><code>enable</code>设置是否开启回到顶部按钮</p>
</li>
<li><p><code>sidebar</code> 按钮会出现在侧栏下方</p>
</li>
<li><p><code>scrollpercent</code> 显示阅读百分比</p>
</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">back2top:</span><br><span class="line">  enable: true</span><br><span class="line">  sidebar: false</span><br><span class="line">  scrollpercent: false</span><br></pre></td></tr></table></figure>

<h3 id="书签：定位到上次阅读的地方"><a href="#书签：定位到上次阅读的地方" class="headerlink" title="书签：定位到上次阅读的地方"></a>书签：定位到上次阅读的地方</h3><p><code>主题配置文件</code>中的<code>bookmark</code>部分：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">bookmark:</span><br><span class="line">  enable: true</span><br><span class="line">  # Customize the color of the bookmark.</span><br><span class="line">  color: &quot;#222&quot;</span><br><span class="line">  # If auto, save the reading progress when closing the page or clicking the bookmark-icon.</span><br><span class="line">  # If manual, only save it by clicking the bookmark-icon.</span><br><span class="line">  save: auto</span><br></pre></td></tr></table></figure>

<h3 id="版权信息"><a href="#版权信息" class="headerlink" title="版权信息"></a>版权信息</h3><p><code>主题配置文件</code>中的<code>bookmark</code>部分：</p>
<ul>
<li><code>license:</code>设置版权标准</li>
<li><code>sidebar:</code>设置是否在侧边栏显示版权标准</li>
<li><code>post:</code>设置是否在文章底部显示版权信息</li>
<li><code>language:</code>设置语言</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">creative_commons:</span><br><span class="line">  license: by-nc-sa</span><br><span class="line">  sidebar: false</span><br><span class="line">  post: false</span><br><span class="line">  language:</span><br></pre></td></tr></table></figure>

<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="设置「背景动画」"><a href="#设置「背景动画」" class="headerlink" title="设置「背景动画」"></a>设置「背景动画」</h3><ol>
<li><p>首先在<code>~\themes\hexo-theme-next\source\lib</code>下载</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;theme-next&#x2F;theme-next-three three</span><br><span class="line">git clone https:&#x2F;&#x2F;github.com&#x2F;theme-next&#x2F;theme-next-canvas-ribbon canvas-ribbon</span><br></pre></td></tr></table></figure>
</li>
<li><p>更改<code>主题配置文件</code>中开启一种即可</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">three:</span><br><span class="line">  enable: false</span><br><span class="line">  three_waves: false</span><br><span class="line">  canvas_lines: false</span><br><span class="line">  canvas_sphere: false</span><br><span class="line">#在背景随机出现彩带，鼠标点击后更换</span><br><span class="line">canvas_ribbon:</span><br><span class="line">  enable: ture</span><br><span class="line">  size: 300 # The width of the ribbon</span><br><span class="line">  alpha: 0.6 # The transparency of the ribbon</span><br><span class="line">  zIndex: -1 # The display level of the ribbon</span><br></pre></td></tr></table></figure>
</li>
<li><p>如果是使用CDN而不是第二种，则在<code>vendors</code>模块中添加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">three: &#x2F;&#x2F;cdn.jsdelivr.net&#x2F;gh&#x2F;theme-next&#x2F;theme-next-three@1&#x2F;three.min.js</span><br><span class="line">three_waves: &#x2F;&#x2F;cdn.jsdelivr.net&#x2F;gh&#x2F;theme-next&#x2F;theme-next-three@latest&#x2F;three-waves.min.js</span><br><span class="line">canvas_lines: &#x2F;&#x2F;cdn.jsdelivr.net&#x2F;gh&#x2F;theme-next&#x2F;theme-next-three@latest&#x2F;canvas_lines.min.js</span><br><span class="line">canvas_sphere: &#x2F;&#x2F;cdn.jsdelivr.net&#x2F;gh&#x2F;theme-next&#x2F;theme-next-three@latest&#x2F;canvas_sphere.min.js</span><br></pre></td></tr></table></figure>

</li>
</ol>
<p>其他动画的设置：<a href="https://github.com/hustcc/canvas-nest.js/blob/master/README-zh.md">canvas-nest</a>：</p>
<ol>
<li><p>在~\themes\hexo-theme-next\source\lib目录下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">git clone git@github.com:hustcc&#x2F;canvas-nest.js.git canvas-nest</span><br></pre></td></tr></table></figure>
</li>
<li><p>在<code>主题配置文件</code>中增加：</p>
<ul>
<li><code>color</code>： 线条颜色</li>
<li><code>opacity</code>：透明度（0-1）</li>
<li><code>count</code>：线条总数目</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># Canvas-nest</span><br><span class="line"># Dependencies: https:&#x2F;&#x2F;github.com&#x2F;theme-next&#x2F;theme-next-canvas-nest</span><br><span class="line"># For more information: https:&#x2F;&#x2F;github.com&#x2F;hustcc&#x2F;canvas-nest.js</span><br><span class="line">canvas_nest:</span><br><span class="line">  enable: true</span><br><span class="line">  onmobile: true # Display on mobile or not</span><br><span class="line">  color: &quot;0,0,255&quot; </span><br><span class="line">  opacity: 0.5 </span><br><span class="line">  zIndex: -1 # z-index property of the background</span><br><span class="line">  count: 99 # The number of lines</span><br></pre></td></tr></table></figure>



</li>
</ol>
<h3 id="右上角出现Github角标"><a href="#右上角出现Github角标" class="headerlink" title="右上角出现Github角标"></a>右上角出现Github角标</h3><p>点击之后可以跳转到其源码页面，修改<code>主题配置文件</code>中<code>github_banner</code>：</p>
<ul>
<li><code>enable</code>设置是否在页面右上角显示Github三角标</li>
<li><code>permalink:</code>填写跳转到的Github地址</li>
<li><code>title:</code>设置鼠标移动到图标后显示的文字</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">github_banner:</span><br><span class="line">  enable: false</span><br><span class="line">  permalink: https:&#x2F;&#x2F;github.com&#x2F;yourname</span><br><span class="line">  title: Follow me on GitHub</span><br></pre></td></tr></table></figure>

<h3 id="页脚"><a href="#页脚" class="headerlink" title="页脚"></a>页脚</h3><p>修改<code>主题配置文件</code>中<code>footer</code>：</p>
<ul>
<li><code>since</code>表示网站成立的年份，不设置的话默认是当前年份</li>
<li><code>icon</code>设置年份后面的图标<ul>
<li><code>name</code>默认是心形</li>
<li><code>animated</code>用于设置心形是否跳动</li>
<li><code>color</code>设置图标颜色</li>
</ul>
</li>
<li><code>copyright</code>显示图标后面的版权所属，也就是作者名，不设置的话默认显示根目录配置文件中的作者名</li>
<li><code>powered</code>设定是否显示Powered by Hexo以及当前主题的版本信息</li>
</ul>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">footer:</span><br><span class="line">  #since: 2015</span><br><span class="line">  icon:</span><br><span class="line">    name: fa fa-heart</span><br><span class="line">    animated: false</span><br><span class="line">    color: &quot;#ff0000&quot;</span><br><span class="line">  copyright:</span><br><span class="line">  powered: true</span><br><span class="line"></span><br><span class="line">  # Beian ICP and gongan information for Chinese users. See: https:&#x2F;&#x2F;beian.miit.gov.cn, http:&#x2F;&#x2F;www.beian.gov.cn</span><br><span class="line">  beian:</span><br><span class="line">    enable: false</span><br><span class="line">    icp:</span><br><span class="line">    # The digit in the num of gongan beian.</span><br><span class="line">    gongan_id:</span><br><span class="line">    # The full num of gongan beian.</span><br><span class="line">    gongan_num:</span><br><span class="line">    # The icon for gongan beian. See: http:&#x2F;&#x2F;www.beian.gov.cn&#x2F;portal&#x2F;download</span><br><span class="line">    gongan_icon_url:</span><br></pre></td></tr></table></figure>

<h3 id="标签的-改成图标"><a href="#标签的-改成图标" class="headerlink" title="标签的#改成图标"></a>标签的#改成图标</h3><p>将在<code>主题配置文件</code>中的<code>tag_icon</code>改为<code>true</code>即可</p>
<h3 id="归档页面年份后增加十二生肖"><a href="#归档页面年份后增加十二生肖" class="headerlink" title="归档页面年份后增加十二生肖"></a>归档页面年份后增加十二生肖</h3><ol>
<li><p><a href="https://oss.chenjunxin.com/files/blogfiles/9ec4151c_chinese-zodiac.zip">下载十二生肖字体</a>，将解压的三个字体文件全部放在根目录 <del>/source/fonts/ 下，在</del>\themes\hexo-theme-next\layout_macro\post-collapse.swig文件中，修改 <code>&#123;%- if year !== current_year %&#125;</code>部分为</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;%- if year !&#x3D;&#x3D; current_year %&#125;</span><br><span class="line">  &#123;%- set current_year &#x3D; year %&#125;</span><br><span class="line">  &lt;div class&#x3D;&quot;collection-year&quot;&gt;</span><br><span class="line">    &lt;&#123;%- if theme.seo %&#125;h2&#123;% else %&#125;h1&#123;%- endif %&#125; class&#x3D;&quot;collection-header&quot;&gt;&#123;&#123; current_year &#125;&#125;</span><br><span class="line">      &lt;div class&#x3D;&quot;chinese-zodiac&quot;&gt;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 0 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-monkey&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 1 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-rooster&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 2 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-dog&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 3 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-pig&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 4 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-rat&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 5 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-ox&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 6 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-tiger&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 7 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-rabbit&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 8 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-dragon&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 9 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-snake&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 10 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-horse&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">        &#123;%- if current_year % 12 &#x3D;&#x3D; 11 %&#125;</span><br><span class="line">          &lt;i class&#x3D;&quot;symbolic-animals icon-goat&quot;&gt;&lt;&#x2F;i&gt;</span><br><span class="line">        &#123;%- endif %&#125;</span><br><span class="line">      &lt;&#x2F;div&gt;</span><br><span class="line">    &lt;&#x2F;&#123;%- if theme.seo %&#125;h2&#123;% else %&#125;h1&#123;%- endif %&#125;&gt;</span><br><span class="line">  &lt;&#x2F;div&gt;</span><br><span class="line">&#123;%- endif %&#125;</span><br></pre></td></tr></table></figure>
</li>
<li><p>在~/source/_data/styles.styl中，增加</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.chinese-zodiac &#123;</span><br><span class="line">    float: right;</span><br><span class="line">&#125;</span><br><span class="line">@font-face &#123;</span><br><span class="line">  font-family: &#39;chinese-zodiac&#39;;</span><br><span class="line">  font-display: swap;</span><br><span class="line">  src: url(&#39;&#x2F;fonts&#x2F;chinese-zodiac.eot&#39;);</span><br><span class="line">  src: url(&#39;&#x2F;fonts&#x2F;chinese-zodiac.eot&#39;) format(&#39;embedded-opentype&#39;),</span><br><span class="line">       url(&#39;&#x2F;fonts&#x2F;chinese-zodiac.woff2&#39;) format(&#39;woff2&#39;),</span><br><span class="line">       url(&#39;&#x2F;fonts&#x2F;chinese-zodiac.woff&#39;) format(&#39;woff&#39;);</span><br><span class="line">  font-weight: normal;</span><br><span class="line">  font-style: normal;</span><br><span class="line">&#125;</span><br><span class="line">.symbolic-animals &#123;</span><br><span class="line">  display: inline-block;</span><br><span class="line">  font: normal normal normal 14px&#x2F;1 chinese-zodiac;</span><br><span class="line">  font-size: inherit;</span><br><span class="line">  text-rendering: auto;</span><br><span class="line">  -webkit-font-smoothing: antialiased;</span><br><span class="line">  -moz-osx-font-smoothing: grayscale;</span><br><span class="line">&#125;</span><br><span class="line">.icon-dragon:before &#123; content: &#39;\e806&#39;; &#125;</span><br><span class="line">.icon-tiger:before &#123; content: &#39;\e809&#39;; &#125;</span><br><span class="line">.icon-pig:before &#123; content: &#39;\e810&#39;; &#125;</span><br><span class="line">.icon-horse:before &#123; content: &#39;\e813&#39;; &#125;</span><br><span class="line">.icon-rat:before &#123; content: &#39;\e816&#39;; &#125;</span><br><span class="line">.icon-goat:before &#123; content: &#39;\e818&#39;; &#125;</span><br><span class="line">.icon-snake:before &#123; content: &#39;\e820&#39;; &#125;</span><br><span class="line">.icon-ox:before &#123; content: &#39;\e822&#39;; &#125;</span><br><span class="line">.icon-dog:before &#123; content: &#39;\e825&#39;; &#125;</span><br><span class="line">.icon-rabbit:before &#123; content: &#39;\e826&#39;; &#125;</span><br><span class="line">.icon-monkey:before &#123; content: &#39;\e829&#39;; &#125;</span><br><span class="line">.icon-rooster:before &#123; content: &#39;\e82f&#39;; &#125;</span><br></pre></td></tr></table></figure>



</li>
</ol>
<h3 id="增加网页的小动物"><a href="#增加网页的小动物" class="headerlink" title="增加网页的小动物"></a>增加网页的小动物</h3><ol>
<li><p>在~下<code>npm install -save hexo-helper-live2d</code>，并在<code>站点配置文件</code>中输入：</p>
<ul>
<li><p><code>NAME</code>可选：<code>chitose</code>、<code>epsilon2_1</code>、<code>haru/01</code>、<code>haru/02</code>、<code>haruto</code>、<code>hibiki</code>、<code>hijiki</code>、<code>izumi</code>、<code>koharu</code>、<code>miku</code>、<code>ni-j</code>、<code>nico</code>、<code>nietzsche</code>、<code>nipsilon</code>、<code>nito</code>、<code>shizuku</code>、<code>tororo</code>、<code>tsumiki</code>、<code>unitychan</code>、<code>wanko</code>、<code>z16</code></p>
</li>
<li><p><code>display</code>中：<code>width</code>和<code>height</code>分别控制宽度和高度，<code>position</code>控制位置，<code>hOffset</code>和<code>vOffset</code>分别控制水平和垂直的偏移</p>
</li>
<li><p> <code>mobile</code>中，<code>show</code>控制是否在移动设备上显示，<code>scale</code>控制在移动设备上的缩放</p>
</li>
<li><p> <code>react</code>中<code>opacity</code>控制透明度</p>
</li>
<li></li>
</ul>
</li>
</ol>
   <figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">live2d:</span><br><span class="line">  enable: true</span><br><span class="line">  scriptFrom: local</span><br><span class="line">  pluginRootPath: live2dw&#x2F;</span><br><span class="line">  pluginJsPath: lib&#x2F;</span><br><span class="line">  pluginModelPath: assets&#x2F;</span><br><span class="line">  tagMode: false</span><br><span class="line">  log: false</span><br><span class="line">  model:</span><br><span class="line">    use: live2d-widget-model-NAME</span><br><span class="line">  display:</span><br><span class="line">    hOffset: 0</span><br><span class="line">    vOffset: -20</span><br><span class="line">    position: right</span><br><span class="line">    width: 150</span><br><span class="line">    height: 300</span><br><span class="line">  mobile:</span><br><span class="line">    show: true</span><br><span class="line">    scale: 0.5</span><br><span class="line">  react:</span><br><span class="line">    opacity: 1.0</span><br></pre></td></tr></table></figure>

<ol start="2">
<li>在~新建文件夹/live2d_models/NAME，在NAME文件夹下，新建文件NAME.model.json，然后安装模型<code>npm install --save live2d-widget-model-NAME</code></li>
</ol>
]]></content>
      <categories>
        <category>网站</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>git</tag>
      </tags>
  </entry>
</search>
